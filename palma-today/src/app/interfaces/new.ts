export interface New {
  header: string;     // Header of the new
  lang: string;       //language of the new
  section?: string;   // Section of the new
  date?: Date;        // Publication date
  link?: string;      // URL to the new
}
